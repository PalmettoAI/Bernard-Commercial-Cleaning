FROM python:3.11-slim
RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*
RUN pip install --no-cache-dir anthropic google-auth cryptography
WORKDIR /app
COPY . .
CMD ["python3", "weekly_generator.py"]
